# Client (Vite — exposed to the browser)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Server (Server Functions — never commit real values)
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Match sync (football-data.org — optional; admin can enter matches manually)
FOOTBALL_DATA_TOKEN=
